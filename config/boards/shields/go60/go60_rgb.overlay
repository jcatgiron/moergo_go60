#include <dt-bindings/zmk/rgb.h>

/ {
    chosen {
        zmk,underglow = &rgb_underglow;
    };

    rgb_underglow: rgb_underglow {
        compatible = "zmk,underglow";
        status = "okay";

        chain-length = <60>;
        brightness = <120>;
        current-brightness = <150>;

        default-effect = <RGB_EFF_SOLID>;
        speed = <2>;
    };
};
